---
label: _navigation
hide_body: true
fields:
- name: header
  type: list
  config:
    use_select: true
    min: 1
    max: 5
    source:
      type: pages
  label: Header
  description: Header navigation links.
- name: columns
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
  label: Columns
  description: A list of columns displayed inline (usually at the bottom of the page).
- name: footer
  type: list
  config:
    use_select: true
    min: 
    max: 5
    source:
      type: pages
  label: Footer
  description: Footer navigation links.
pages:
- content/navigation.json
