---
label: _calendar
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: Calendar name
- name: entries
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
  label: Calendar Entries
