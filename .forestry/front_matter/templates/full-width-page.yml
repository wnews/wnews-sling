---
label: WebPage
hide_body: true
fields:
- name: creativework
  type: include
  config: {}
  template: creativework
  label: CreativeWork
- name: breadcrumb
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
  label: Breadcrumb
- name: relatedLink
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
  label: Related
