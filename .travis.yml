cache:
  directories:
    - $HOME/.gradle/wrapper/
sudo: required
services:
  - docker
dist: trusty
language: java
jdk:
  - oraclejdk8
deploy:
  provider: script
  script: scripts/publish-docker-image.sh
  on:
    branch: master
